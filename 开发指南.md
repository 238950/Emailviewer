# 学生邮件查看器 · 开发指南

面向想读代码、改功能、继续维护的同学。先读本文件，再按需深入对应源码。

---

## 0. 技术栈与设计原则

| 层 | 技术 |
|---|---|
| 后端 | Node.js ≥ 22（ESM）+ Express 4 + SQLite（Node 内置 `node:sqlite`，零原生编译） |
| 收信协议 | `imapflow`（IMAP）+ `mailparser`（MIME 解析） |
| 经典 Outlook 桌面 | PowerShell 脚本 → Outlook COM 自动化（见 §6）。**仅经典 Outlook（classic）可用**；新版 Outlook（`1.2026.x`，WebView2）**没有 COM 接口**，无法自动化 |
| AI | OpenAI 兼容 HTTP 接口（`fetch`），本地关键词规则兜底 |
| 前端 | React 18 + Vite + zustand；无路由库，`#view=` 深链 + 状态切换 |
| 部署形态 | 单机本地服务：后端同时托管前端 `web/dist`，浏览器访问 `http://127.0.0.1:3869`（**3869 为服务端口**，可用 `MAILVIEW_PORT` 覆盖） |

设计要点：
1. **只读阅读器**：不实现写信/回复；邮件状态（已读/星标）尽力写回数据源；
2. **“信封先行，正文后置”**：列表只需信封（快）；正文/附件按需或后台限速抓取（省流量、不卡 UI）；
3. **双数据源共享落库**：IMAP 与经典 Outlook 桌面同步到同一套 `message/attachment` 表，上层（AI、规则、日历、附件库、搜索、UI）完全复用。**默认/推荐数据源是 IMAP 云端直连**；经典 Outlook 桌面（要求本机安装并登录 classic）只是替代方案，新版 Outlook 因无 COM 接口不能作为数据源；
4. 服务只绑定 `127.0.0.1`（端口 **3869**）；IMAP 密码本地 AES 加密；经典 Outlook 模式不存任何密码（凭据由本机 Outlook 管理）。

## 1. 目录与每个文件的用途

### 根目录
| 文件 | 用途 |
|---|---|
| `EmailViewer.exe` | **唯一启动入口**（C# 启动器，源码 `tools\EmailViewerLauncher.cs`）：无参=首次装依赖 + 后台启动服务（端口 **3869**）+ 打开浏览器 `http://127.0.0.1:3869`；`silent`=仅后台静默启动（供开机自启）；`debug`=带可见控制台窗口启动；`stop`=结束占用 3869 端口的服务进程；`status`=弹窗显示运行状态与地址。**项目已不再包含任何批处理 / 脚本启动器，启停一律通过该 EXE 的子命令完成。** |
| `tools/EmailViewerLauncher.cs` | 启动器源码；重新编译：`csc /target:winexe /out:EmailViewer.exe tools\EmailViewerLauncher.cs`（用系统自带 .NET 4.x，无需额外 SDK） |
| `launcher.log` | 启动器自身日志（启停 / 自启动作记录）；服务日志为 `server/data/server.log` |
| `package.json` | 根脚本（仅收口用） |
| `README.md` | 简介与快速开始 |
| `使用指南.md` | 用户操作手册 |
| `开发指南.md` | 本文档 |
| `dev/` | 开发期脚本（如点击级 UI 回归 `e2e-ui.mjs`） |
| `.npm-cache/` | 离线安装缓存目录（避免沙箱/网络环境写用户目录失败） |

### `server/`
| 文件 | 职责 |
|---|---|
| `src/index.js` | **服务入口**。读设置取端口（用 `MAILVIEW_PORT`，不用通用 `PORT`，防止被宿主环境变量污染）；挂 `/api` 路由；托管 `web/dist` 静态文件；启动后台服务；`OPEN_BROWSER=1` 时自动打开浏览器 |
| `src/api.js` | **全部 REST 路由**（一个文件集中管理）。含：账户 CRUD、同步、文件夹、邮件查询/详情/状态操作、附件、事件/日历、规则、AI 配置、设置、状态、通知、汇总、存储、日志、Outlook 探测端点。自定义轻量 JSON body 解析（免额外依赖） |
| `src/store.js` | **SQLite 数据访问层**。表：`account/folder/message/attachment/rule/event/reminder/setting`。提供 `AccountStore/FolderStore/MessageStore/AttachmentStore/RuleStore/EventStore/SettingsStore`，以及语句缓存 `q/q1/run/tx`。注意：`node:sqlite` 不支持 JS 布尔绑定，所有写库都需转 0/1；`MessageStore.update` 有驼峰→下划线键别名 |
| `src/imap.js` | IMAP 引擎：`syncAccount(accountId)` 是**同步总入口**（按 kind 分发到 outlook）；`walkStructure` 从 `bodyStructure` 提取附件元信息（不必下载正文）；信封增量同步、`fetchMessageSource` 抓原文、`setFlags` 写回（IMAP 与 Outlook 分流）、`testConnection` |
| `src/outlook.js` | **Outlook 桌面数据源**（COM）：探测账号/列文件夹/增量信封/取正文+附件/写回已读星标/断点续传同步；系统文件夹过滤（SYSTEM_TOP）；执行器把生成的 PowerShell 脚本写入 `data/ps`，用 `powershell.exe` 运行并解析 `JSON>>` 行；单飞队列防止并发 COM；记录 Outlook 是否已运行以决定是否 `Quit()`（避免关掉用户打开的 Outlook） |
| `src/hydrate.js` | 正文落库中枢：`hydrateByUid` 按账户类型分流（IMAP→抓 RFC822→`mailparser`；Outlook→COM 取 parts）；共同收尾 `finalizeParsed()`：写正文/附件/头/摘要 → 关键词分类 → 本地摘要 → 应用用户规则。附件落盘、重抓幂等（先删旧附件行） |
| `src/ai.js` | `activeProvider/chat/extractJson`：OpenAI 兼容调用；`aiClassifyBatch` 批量分类+一句话摘要（一次请求完成，JSON 结果按 id 回填）；`classifyByKeywords/summarizeLocally/keywordSummary` 无 AI 兜底；`testProvider` |
| `src/rules.js` | 用户过滤规则引擎：条件（发件人/收件人/主题/正文/附件/文件夹/账户/未读…）× 动作（标签/归类/已读/星标）；`applyRulesToHydrated` 批量重算 |
| `src/nlp.js` | 中英文日期/时间识别（纯规则，无外部依赖）：今天/明天/下周一/周X/月日年/英文月份/时间点 + 触发词（截止/提交/考试/会议…）关联，返回候选 |
| `src/services.js` | 后台定时器（30 秒一拍）：自动同步、到期提醒→通知、正文预取、AI 分类、每日重点汇总（到点后 15 分钟窗口内生成一次）、附件缓存超限清理（删最旧并置 `body_fetched=0` 以便重抓） |
| `src/settings.js` | 默认设置 + 深合并 `updateSettings` + 分类名/颜色 + 邮箱服务商 IMAP 预设表 |
| `src/util.js` | 通用：时间戳、JSON 安全解析、地址列表、文件夹名中文映射(`prettyFolder`)、附件分组、HTML→文本、截断、关键词摘要兜底 |
| `src/crypto.js` | AES-256-GCM 加解密 IMAP 密码；密钥 `data/secret.key`（本机防误泄露，非绝对安全） |
| `src/logger.js` | 控制台+文件日志；导出路径常量（`DATA_DIR/ATTACH_DIR/SAVED_DIR/PUBLIC_DIR`） |
| `data/` | 运行数据，自动生成：`mailview.db`（SQLite WAL）、`attachments/`、`ps/`（临时 ps1）、`saved/`、`server.log` |

### `web/src/`
| 文件/组件 | 职责 |
|---|---|
| `main.jsx` | React 挂载 |
| `App.jsx` | 壳：引导(bootstrap)、主题应用、10 秒状态轮询、视图分发、`#view=` 深链 |
| `store.js` | zustand 全局状态：账户/状态/设置/主题/当前选中/筛选、`refreshStatus`（JSON 比较去抖，避免每轮全树抖动）、toast、分类名/色辅助 |
| `api.js` | fetch 封装 + 查询串构造 + 附件 URL + 时间/大小格式化工具 |
| `index.css` | 全部样式：CSS 变量双主题、布局、组件类 |
| `components/common.jsx` | 按钮/徽章/弹窗/抽屉/菜单/字段/开关/Toast |
| `components/TopBar.jsx` | 顶栏：视图切换、刷新全部、添加账户、提醒铃铛（每 20 秒轮询通知）、主题切换 |
| `components/FolderColumn.jsx` | 左栏：账户（主展开/副折叠圆点+未读）与文件夹树、单账户同步按钮 |
| `components/MailListView.jsx` | 通用邮件列表：筛选/搜索/排序/分组/分页、行内已读/星标/菜单、标签弹窗；通过 `queryBase` 复用于邮箱/智能收件箱 |
| `components/ReaderPane.jsx` | 阅读窗格：头信息、HTML 消毒+cid 图片重写+远程图片拦截、纯文本回退、AI 摘要/分类按钮、附件预览/下载/保存、日期候选一键加日历、事件展示/删除 |
| `components/NewAccountModal.jsx` | 添加账户向导：**IMAP 云端直连（推荐，默认选中）** 与 **经典 Outlook 桌面（classic，替代方案；新版 Outlook 无 COM 接口）** 两种数据源 |
| `views/MailView.jsx` | 邮箱视图三栏布局 |
| `views/SmartView.jsx` | 智能收件箱：账户多选 + 分类过滤（复用 MailListView） |
| `views/AttachmentsView.jsx` | 附件库：类型分组统计、搜索、大附件、预览 |
| `views/CalendarView.jsx` | 内置日历月视图、新建/删除/详情、.ics 导出 |
| `views/SettingsView.jsx` | 设置页全部子页：账户、同步、AI、规则、汇总、存储、外观、日志 |

## 2. 数据库模型（表）

```
account(id, kind[imap|outlook-local], name, email, username, host, port, ssl,
        password_enc, is_primary, color, enabled, last_sync_at, sync_state, sync_error, extra)
folder(account_id, name(路径), delim, flags(JSON: IMAP 标记数组 / Outlook:{entryId,storeId,cutoff}),
       total, unread, highest_uid, uidvalidity, synced_at)   PK(account_id,name)
message(id, account_id, folder, uid, msg_id, subject, from_addr, from_name, to_list, cc_list,
        date_ms, received_ms, size, flags, read, important, has_attachments, att_json,
        snippet, body_text, body_html, body_fetched, headers_json,
        category, category_reason, category_model, category_at, ai_attempted,
        ai_summary, ai_summary_model, ai_summary_at, labels, created_at)  UNIQUE(account_id,folder,uid)
attachment(id, message_id, account_id, filename, mime, size, content_id, disposition, part,
           stored(相对路径), hash, created_at)
rule(id, name, enabled, priority, match_json, action_json)
event(id, title, start_ms, end_ms, all_day, source, message_id, note, color, remind_offsets)
reminder(id, event_id, due_ms, fired, fired_at)
setting(key, value JSON)
```

要点：
- Outlook 账户的 `uid` = 对 `msg_id`（= EntryID）做的 CRC32 哈希；`msg_id` 存原始 EntryID 供 COM 取件；
- Outlook 的文件夹 `name` 存 Outlook `FolderPath`（保证唯一），`flags` 存 `{entryId, storeId, cutoff}`（cutoff 为增量同步断点）；
- 分类字段由「本地关键词」或「AI」写入；`ai_attempted` 表示是否尝试过 AI（避免无 AI 时反复空跑）。

## 3. 主要数据流

```
添加账户向导
  → POST /api/accounts  (kind: outlook-local | imap)
  → 创建 account 行 → POST /api/accounts/:id/sync（即 syncAccount）
syncAccount (imap.js)
  ├─ kind=imap:         文件夹清单 → 每文件夹增量信封（fetch envelope/bodyStructure）→ 入库
  └─ kind=outlook-local: outlook.js.syncOutlookAccount
         outlookFolders(COM) → 过滤系统目录 → upsert folder(含 cutoff 断点)
         → 每文件夹 outlookEnvelopes(COM 只读信封) → 入库（含状态刷新）

后台 services.js（每 30s）：
  runHydration：对 body_fetched=0 的最新 N 封 → hydrateByUid（打开邮件时也会即时触发）
  runAiClassify：AI 开启时对 ai_attempted=0 且正文已抓的邮件批量分类+摘要
  其余：到期提醒→铃铛、每日汇总、附件超限清理

阅读一封邮件：
  GET /api/messages/:id → hydrateByUid → finalizeParsed
    （写 body/附件/头/摘要 → 关键词分类 → 本地摘要 → 用户规则）
  POST /api/messages/:id/read → 本地更新 + setFlags 写回数据源（IMAP flags / Outlook UnRead）
UI 状态：
  GET /api/status（10s 轮询，未读数/同步状态） + GET /api/notifications（20s，铃铛）
```

## 4. REST API 一览（前缀 `/api`）

| 分组 | 端点 |
|---|---|
| 健康/预设 | `GET /health` `GET /presets` |
| 账户 | `GET/POST /accounts`、`PUT/DELETE /accounts/:id`、`POST /accounts/test`、`POST /accounts/:id/primary`、`POST /accounts/:id/sync`、`GET /accounts/:id/folders` |
| Outlook | `GET /outlook/status`（探测+列账号）、`POST /outlook/folders`（{email,name}） |
| 邮件 | `GET /messages`（组合过滤/排序/分组/分页参数）、`GET /messages/:id`（自动抓正文）、`POST /messages/:id/read|flag|label|classify|summarize`、`GET /messages/:id/dates`（日期候选）、`POST /messages/batch-action`、`POST /messages/:id/rules-preview` |
| 分类/状态 | `GET /categories`、`GET /status` |
| 附件 | `GET /attachments`、`GET /attachments/groups`、`GET /attachments/:id/inline|download`、`POST /attachments/:id/save` |
| 日历 | `GET/POST /events`、`PUT/DELETE /events/:id`、`GET /reminders` |
| 规则 | `GET/POST /rules`、`PUT/DELETE /rules/:id`、`POST /rules/apply` |
| AI | `GET/PUT /ai/config`、`POST /ai/test`、`POST /ai/classify`、`GET /ai/status` |
| 设置/存储 | `GET/PUT /settings`、`POST /settings/save-dir-test`、`GET /storage`、`POST /storage/purge-cache`、`GET /logs`、`GET /system` |
| 汇总/通知 | `GET /digest/latest`、`POST /digest/now|ack|reset`、`GET/POST /notifications` |

参数风格示例：`GET /api/messages?accountIds=uuid1,uuid2&folder=INBOX&unread=true&q=高数&category=assignment&sort=date&dir=desc&page=0&pageSize=80`。
返回统一 `{ok:true,...}` 或 `{ok:false,error}`。

## 5. 开发调试

```powershell
# 后端热重启（监听 src 变更）
cd server; npm run dev        # http://127.0.0.1:3869（前端用构建产物或 vite dev）

# 前端开发（代理 /api → 3869）
cd web; npm run dev           # http://localhost:5173

# 构建前端（server 会托管 dist）
cd web; npm run build
```

快速手工验证（PowerShell）：

```powershell
Invoke-RestMethod http://127.0.0.1:3869/api/status
Invoke-RestMethod -Method Post http://127.0.0.1:3869/api/outlook/status
```

点击级界面回归（自动化，需服务运行 + 本机 Chrome）：

```powershell
node dev/e2e-ui.mjs    # 真实点击：切副账号→列表即时出现→打开邮件预览→智能收件箱标签筛选+预览
```

> 运行 / 停止本机服务也可直接用启动器，无需批处理脚本：`EmailViewer.exe`（无参=装依赖 + 后台启动 + 开浏览器）、`EmailViewer.exe debug`（带可见控制台，便于实时看日志）、`EmailViewer.exe silent`（仅后台启动）、`EmailViewer.exe stop`（结束占用 **3869** 端口的进程）、`EmailViewer.exe status`（查看运行状态与地址）。

日志在 `server/data/server.log`；SQLite 可用任意客户端打开 `server/data/mailview.db` 检查（改动时先停服务）。

> 提示：在本机“受限/沙箱”环境里，`npm install` 建议加 `--cache <工作目录>\.npm-cache`；esbuild 相关安装失败可加 `--ignore-scripts`；`vite build` 需要能 spawn esbuild 子进程（改用 `EmailViewer.exe debug` 启动或直接在普通命令行运行 npm 均无此限制）。

## 6. 经典 Outlook COM 实现细节（重要；仅 classic 支持）

- 原理：Node 无法直接 COM，故把参数化 **PowerShell 脚本**写入 `server/data/ps/*.ps1`（UTF-8 **带 BOM**，PS5 才认），`spawn('powershell.exe', [...])` 执行，脚本用 `Write-Output ('JSON>>' + ConvertTo-Json)` 输出结构化行，Node 按行解析。
- **并发**：模块内 `psQueue` 串行化；`activePid` 供 `/outlook/status` 判断是否忙碌。
- **不误关用户 Outlook**：脚本开头记录 `$__wasRunning = Get-Process OUTLOOK 是否存在`；仅当原本未运行时才 `$ol.Quit()`。
- **超时**：COM 冷启动可能 30~60 秒，各操作超时 120~240 秒；超时会 kill 子进程并报中文提示。
- 关键 COM 对象/属性：`Application/GetNamespace('MAPI')`、`Accounts[].DeliveryStore`、`GetFolderFromID(entryId,storeId)`、`Items.Sort('[ReceivedTime]',$true)`（倒序便于增量 `break`）、邮件类 `Class -eq 43`（olMail）、`EntryID/Subject/SenderName/SenderEmailAddress/ReceivedTime/Size/UnRead/FlagStatus/Categories/Attachments`、正文 `Body/HTMLBody`、附件 `SaveAsFile`、写回 `UnRead=$false` 与 `FlagStatus 0/1`。
- 限制：**只对经典 Outlook（classic）有效**——新版 Outlook（`1.2026.x`，WebView2 架构）不暴露 COM/MAPI 自动化接口，无法作为数据源，官方推荐做法是改用 IMAP 云端直连；Outlook 对象模型可能弹“某程序正在访问 Outlook”提示（与本机信任设置有关）；拿不到原始 RFC822 邮件头（因此头信息为空属正常）；只能访问 Outlook 客户端本地已下载的缓存范围。

## 7. 常见二次开发点

- **加一个分类**：改 `server/src/settings.js` 的 `categories/categoryColors` → `web/src/store.js`（若需硬编码处）→ `server/src/ai.js` 的 `CATEGORY_KEYS` 与提示词。
- **加邮件服务商预设**：`settings.js` 的 `HOST_PRESETS`。
- **加 AI 服务商**：`settings.js` 的 `ai.providers`（任意 OpenAI 兼容端点）。
- **加过滤条件/动作**：`rules.js`（`FIELD_ALIAS`、`matchCondition`、动作枚举）→ 前端 `SettingsView.jsx` 的 `RULE_FIELDS/describeRule`。
- **加新统计/导出**：优先在 `store.js` 加查询，`api.js` 加路由，UI 侧新视图仿 `views/*`。
- **迁移目录**：所有相对路径均基于本文件位置解析（`logger.js` 的 `fileURLToPath`），把整个文件夹改名/搬走不影响运行。

## 8. 发布/打包

直接把整个 `emailviewer` 目录拷给目标电脑（含 `EmailViewer.exe`、`server/node_modules`、`web/node_modules`、`web/dist`，数据 `server/data` 可留可清）。目标机装好 Node.js ≥ 22 后双击 `EmailViewer.exe` 即可（无参数会先自动装依赖，再后台启动服务并打开 `http://127.0.0.1:3869`）。无需系统服务、无需其他安装步骤（仅“开机自启”会写一条 `HKCU\...\Run` 项指向 `EmailViewer.exe silent`，可在设置里关闭）。
