# 缺陷修复说明（对照《EmailViewer 测试缺陷报告》BUG-01 ~ BUG-40）

本说明记录针对测试报告的 40 项缺陷（含 22 个 BUG 编号 + 轻微缺陷 BUG-24~40）所做的修改与验证方式。
测试数据来自本机两个经典 Outlook 账户（`you@outlook.com`、`user@example.com`）。

验证脚本（都在 `dev/`，可重复运行）：

| 脚本 | 覆盖范围 | 用法 |
| --- | --- | --- |
| `dev/verify-fixes.mjs` | 接口层：BUG-09/16/17/18/19/20/24/25/26/27/29/32/33/34/35/40 | `node dev/verify-fixes.mjs 3869` |
| `dev/verify-ui-fixes.mjs` | 界面层：BUG-05/06/07/08/20/22/30 | `node dev/verify-ui-fixes.mjs` |
| `dev/verify-delete-account.mjs` | 隔离验证 BUG-12（临时库，不碰真实数据） | `node dev/verify-delete-account.mjs 4387 <临时数据目录>` |
| `dev/verify-ics.mjs` | BUG-28（真实导出 .ics 并校验 RFC 5545） | `node dev/verify-ics.mjs` |
| `dev/e2e-ui.mjs` | 端到端 A~F 全流程回归 | `node dev/e2e-ui.mjs` |
| `dev/cleanup-legacy-auto-events.mjs` | 清理旧版误识别产生的噪声日历事件 | `node dev/cleanup-legacy-auto-events.mjs --apply` |

---

## 一、严重缺陷

**BUG-01 每日汇总查不到收件箱（Outlook 模式）**
新增公共解析模块 `server/src/mailboxes.js`（`resolveInboxFolders()` / `isInboxFolderName()`），把「收件箱同义名」统一解析为真实路径（如 `\\账号\Inbox`）。汇总、首页、统一收件箱、批量安排日历全部改用它。

**BUG-02 规则默认停用**
`RuleStore.save()` 未显式传 `enabled` 时默认写入 1（启用），不再出现“新建即停用”。

**BUG-03 规则空值条件恒真**
`server/src/rules.js` 中 `has_attachment` / `unread` 作为布尔字段先判定；文本/枚举条件在取值为空时直接返回 false，不再“空条件匹配所有邮件”。

**BUG-04 内嵌图裂图 / 附件区重复**
`server/src/outlook.js` 读取 `PR_ATTACH_CONTENT_ID` 并写入 `contentId`，附件按 `cid` 标记内联；`ReaderPane.jsx` 增加 cid 未命中时按正文顺序兜底配对、仍无法匹配则显示占位提示（`.img-missing`），并且内嵌图不再在附件区重复列出（`visibleAtts`）。

**BUG-05 文件夹名被截断成同一串**
`pretty.js` 的 `displayName()` 改为只取路径末段并译中文（收件箱 / 已发送邮件 / 垃圾邮件…），新增 `fullName()` 供 hover 显示完整路径。实测 5/5 行不再截断、互不重复。

**BUG-18 停用全部账户后首页仍显示全部邮件**
`/messages`、`/home`、`/home/done`、`/calendar/auto-from-mail` 在“无启用账户/未解析到收件箱”时显式返回空结果（`noAccounts: true`），不再退化为全库查询。

---

## 二、中等缺陷

**BUG-06 账户无法折叠**
`FolderColumn.jsx` 的折叠按钮常驻显示（`ChevronDown ⇄ ChevronRight`），主账户同样可折叠，展开状态由 `expandedAccounts` 统一管理。

**BUG-07 搜索框方向键被全局快捷键劫持**
`MailListView.jsx` 键盘处理开头判断 `INPUT/TEXTAREA/SELECT/contentEditable` 直接返回，并在真正切换时 `preventDefault()`；焦点不在输入框时 ↑/↓ 仍可切换邮件（反向用例一并验证）。

**BUG-08 主题默认值三处不一致**
`store.js` 初始主题改为 `system`，启动时若本地无覆盖则采用后端 `settings.theme`，切换主题会写回后端（`PUT /api/settings`），换浏览器/清缓存后仍记得。

**BUG-09「每次同步后台抓取正文的封数」设置无效**
`services.js` 的 `runHydration()` 读取 `hydrateNewLimit` 并按该值限制单轮抓取量（0 = 不预取），非法值回落默认 60。

**BUG-10「应用到已下载邮件」只扫最近 200 封**
`applyRulesToHydrated()` 改为分页遍历全部已下载邮件（`maxScan` 上限 10000，带进度日志），不再只处理最近 200 封。

**BUG-11 新建 IMAP 账户时端口 / SSL 不生效**
向导改为「选择预设即写入表单，之后以用户输入为准」；服务端 `POST /api/accounts` 把 `allowInsecureTls` 等选项真正落库（此前 `extra` 计算后未写入）；测试连接支持 `allowInsecureTls`。

**BUG-12 删除账户留下孤儿日历事件、不清理附件文件**
`AccountStore.remove()` 在同一事务内清理 folder/message/attachment 与由该账户邮件派生的 event（含 reminder），事务提交后删除磁盘附件文件，并返回 `{messages, attachments, files, bytesFreed, events}`；设置页确认框与提示语如实展示释放量。隔离临时库验证：事件 1→0、文件已删除、接口返回统计。

**BUG-13 新增账户重复同步两遍**
向导不再二次调用 `/api/sync`（`POST /api/accounts` 内部已完成首轮同步 + 正文预取），并用接口返回的 `r.account.id` 选中账户，不再按显示名反查。

**BUG-14 文档指引用户双击不存在的文件**
`README.md`、`使用指南.md`、`开发指南.md` 全部改为 `EmailViewer.exe` 的 `silent / debug / stop / status` 子命令说明，目录树同步修正；全文检索确认无 `start.bat` / `stop.bat` / `start-debug.bat` / `start-silent.vbs` 残留。

**BUG-15 README 与向导对“推荐模式”说法相反**
统一为「IMAP 云端直连」为推荐/默认，经典 Outlook 桌面为替代方案（需已安装并登录经典版、无需邮箱密码），并明确新版 Outlook（WebView2）不提供 COM 接口、无法自动化。

**BUG-16 本地接口无来源校验（CSRF）**
新增 `originGuard` 中间件：带 `Origin`/`Referer` 的请求必须是 `127.0.0.1/localhost` 且端口为本机服务端口，否则 403；写接口必须是 `application/json`，`text/plain` 等可绕过预检的类型返回 415。

**BUG-17 请求体无大小限制**
请求体上限 2MB：先按 `Content-Length` 预判，超限直接 413 并丢弃剩余数据；流式累计超限同样回 413，不再全量读入内存（也不再因 `destroy()` 让客户端只看到连接中断）。

**BUG-19 日期误报污染首页/日历，且“全部加入日历”会重复**
- `nlp.js` 删除「于/时间/安排/计划/schedule」等泛触发词，候选必须与明确的「截止/考试/活动」语义词共现；
- 数字日期要求真实存在（月份 1–12、日存在、年份 2000–2100），并排除处于更长数字串/电话号语境（如 `T: 3943-1526`）；
- 候选新增 `confidence`（截止/考试词紧邻日期 = high），首页「临近截止」与「智能安排」只采信 high 或 AI 识别结果，阅读窗格对“参考”级候选显示置信度标签并提供「忽略」按钮（`POST /messages/:id/dates/clear`）；
- 阅读窗格「全部加入日历」加入与批量接口相同的去重键（`messageId + 小时`），重复点击不再重复建事件；
- 启动回填会用新规则重算历史日期：重算不出高置信度候选即清空旧误报（本机实测：40 封邮件候选从 133 个降到 10 个；清理 102 封旧误报；首页「临近截止」由 15 条噪声变为 1 条真实截止）。

**BUG-20「已阅」与「已读」概念混淆**
`done` 统一改称「已处理（本机归档）」，`read` 保留「已读」（会写回邮箱）。首页卡片按钮、左下角入口、归档弹窗、阅读窗格按钮与提示全部改文案；阅读窗格元信息同时显示「已读/未读」与「已处理（本机归档）」两个状态标签。

**BUG-21 `delete_local` 名为删除实为打标签**
批量动作的 `delete_local` 改为真正从本地视图移除（`hidden = 1`），新增 `unhide_local` 恢复；不再写入会污染标签列表的中文标签。

**BUG-22 统一收件箱默认只勾主账户**
默认勾选全部账户，新增「全选/取消全选」；取消全部勾选时明确提示“未选择任何账户”，不再静默查询第一个账户。

**BUG-23 打开邮件自动标记已读且无开关**
新增设置项 `markReadOnOpen`（设置 → 外观，默认开启），关闭后打开邮件完全不动邮箱未读状态；顶栏「仅查看」说明同步补充。

**BUG-24 对不存在账户“设为主账户”会清空主标记**
接口校验账户存在性，不存在返回 404，不再执行 `makePrimary`。

**BUG-25 删除不存在的事件返回成功**
`DELETE /api/events/:id` 在事件不存在时返回 404，与账户/邮件接口语义一致。

**BUG-26 事件时间校验与落库取值不一致**
新增 `normalizeEventTimes()`：先 `Number()` coerce 再校验（有限数、2000–2100 年区间、`endMs ≥ startMs`），非法值返回 400 而不是落库成 0/负数。

**BUG-27 设置项无类型校验**
`PUT /api/settings` 增加白名单 + 类型/范围校验（`SETTINGS_SPEC`，含 digest 子项），非法值返回 400 并列出原因，不再把 `"abc"` 落库后静默关闭自动同步。

**BUG-28 导出的 .ics 不符合 RFC 5545**
`CalendarView.jsx` 重写 ICS 生成：补 `DTSTAMP`/`CREATED`，对 `, ; \ 换行` 转义，按 75 字节折行（UTF-8 安全），全天事件用 `VALUE=DATE`。真机导出验证 6/6 通过。

**BUG-29 开机自启状态回读中文路径乱码**
改为用 PowerShell 读取注册表并显式以 UTF-8 输出（`reg.exe` 的 stdout 走控制台代码页），实测回读为 `"C:\Users\USERNAME\...\EmailViewer.exe" silent`。

**BUG-30 邮件列表标题重复账户名**
`displayName()` 只取末段后，标题变为 `you@outlook.com · 收件箱`，重复问题消失。

**BUG-31 保存目录示例路径反斜杠被吞**
`SettingsTab` 提示改为 `{'例：C:\\Users\\你的名字\\Documents\\邮件附件'}`，界面正常显示。

**BUG-32 未知 /api 路由返回 HTML 404**
新增 `/api` 兜底中间件，统一返回 `{ok:false, error:"接口不存在：…"}`。

**BUG-33 附件库总数与分组统计口径不一致**
杂项判定落库（`attachment.junk` + 用户改判 `junk_override`），`AttachmentStore.list()` 把账户/搜索/大小/日期/杂项/类型过滤下推 SQL，`stats()` 用同一表达式统计，列表 `total` 与分组之和一致（实测 294 = 294）。

**BUG-34 每次改已读/星标都新建 IMAP 连接**
新增 `setFlagsBatch()`：同一账户的批量操作复用一条连接（按文件夹分组）。`/messages/batch-action` 改为收集后一次性提交。

**BUG-35 分页参数越界处理不合理**
新增 `clampInt()`：非数字/空/负数回落默认值，过大值夹取上限。`pageSize=-1 → 60`、`pageSize=99999 → 200`、`page=-5 → 0`。

**BUG-36 设置页保存行为不一致（AI 页需再次点保存）**
AI 页的开关（启用/自动分类/自动摘要）与服务商切换改为「改动即保存」，文案标注“改动立即保存”；Base URL/模型/Key 等文本仍走「保存 AI 配置」。

**BUG-37 两处实现瑕疵**
① 阅读窗格重试改为先取 `messageId` 再 `selectMessage(null)`/延时重选，不再依赖返回值副作用；
② 手动分类后的摘要来源按实际模型标注（AI 模型名或本地关键词），不再硬编码 `'local'`。

**BUG-38 下载体验与月视图**
新增 `downloadAttachment()`：取回二进制后用隐藏 `<a download>` 触发，不再留下空白标签页，文件名正确；附件库新增「下载本页全部」；月视图末尾补满整周空格。

**BUG-39 汇总“立即生成”提示硬编码 30 小时**
提示文案跟随 `windowHours` 设置，并在生成前先保存当前表单设置。

**BUG-40「杂项附件」判定偏激进且不可逆**
新增 `junkHideAuto` 设置（设置 → 外观）可整体关闭自动归类；附件库每条附件提供「移出杂项 / 归入杂项 / 恢复自动判定」（`POST /api/attachments/:id/junk`），改判记忆在 `junk_override`。

**BUG-41 新邮件通知预览未限定收件箱**
`pushNewMailNotice` 取通知预览时只按账户过滤、未限定文件夹，会把已归档/已移动的旧邮件也算进来，预览内容与"刚收到"语义不符。现复用 `resolveInboxFolders()` 限定到该账户的收件箱（兼容 Outlook 桌面账户的 `\账号\Inbox` 路径）。

**BUG-42 附件库「类型筛选」全表内存过滤**
`AttachmentStore.list()` 原先把 MIME 分组过滤、排序、分页全部放在 SQL 之后的内存里做：先把整表 JOIN 结果读进 JS 再逐行丢弃。现按分组定义翻译成等价的 SQL 条件（`mime IN (...)` / `mime LIKE 'prefix/%'`，`other` 组用 `NOT` 组合排除已知前缀与显式枚举类型），过滤、排序、分页全部下推 SQLite；`total` 改为 `COUNT(*)` 聚合，不再依赖已切片的数组。等价性由 `dev/verify-attach-pushdown.mjs` 逐分组比对（含多组并列、组合条件、排序、分页）验证。

**BUG-43 启动回填逐封查正文**
`startServices()` 的历史邮件回填在循环里对每封待重算的邮件调用 `MessageStore.detail()`，N 封即 N 次额外 SQL 与对象构造，启动瞬间有可感知的同步 IO 尖峰。现先判定真正需要读正文的邮件集合，再用新增的 `MessageStore.bodiesByIds()` 一次批量取回（超过 500 条自动分片），回填循环改用批量结果。等价性与吞吐由 `dev/verify-backfill-batch.mjs` 验证。

**MIME 分组定义统一（一致性）**
`util.js` 与 `store.js` 各有一份 MIME 大类定义且互不一致（前者含 `code` 组并把 `text/html`、`application/json` 归入其中，后者无 `code` 组而把它们归入 `text`），导致附件库类型筛选与过滤规则的 `attachment_group` 匹配对同一附件可能得出不同分组名。现以 `util.js` 的 `MIME_GROUP`/`mimeGroup()` 为唯一事实源，`store.js` 从中导入并转发导出 `mimeGroupName` 以兼容既有调用方；`text/html` 等归入 `text` 组（邮件场景下它们是正文残留而非可下载的代码文件）。一致性由 `dev/verify-mime-group.mjs` 验证。

---

## 三、数据清理

旧版本误识别产生的噪声日历事件由 `dev/cleanup-legacy-auto-events.mjs` 清理：只删除“来源邮件已不存在对应高置信/ AI 时间点”的 `source=auto` 事件，时间点仍成立的则按识别到的真实语境修正标题。本机执行结果：60 → 1（保留 1 条并修正为 `截止報名日期：2026年9月12日(星期六)`）。

## 四、回归状态

- 接口验证 `dev/verify-fixes.mjs`：35/35 通过
- 界面验证 `dev/verify-ui-fixes.mjs`：14/14 通过
- ICS 导出验证 `dev/verify-ics.mjs`：6/6 通过
- 账户删除隔离验证 `dev/verify-delete-account.mjs`：7/7 通过
- 端到端回归 `dev/e2e-ui.mjs`：A（预览）/ B（标签筛选）/ C（AI 设置与模型列表）/ D（AI 对话）/ E（首页与送 AI）/ F（首页唯一归属与已处理流程）全部通过
- MIME 分组一致性 `dev/verify-mime-group.mjs`：32 项用例两套实现零分歧
- 附件查询下推等价性 `dev/verify-attach-pushdown.mjs`：16/16 通过（逐分组与内存过滤比对）
- 回填批量取正文 `dev/verify-backfill-batch.mjs`：6/6 通过（163 封正文逐字节一致）
