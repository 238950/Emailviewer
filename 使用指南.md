# 学生邮件查看器 · 使用指南

本文面向**使用者**，按实际操作顺序说明。若你想看代码结构与二次开发，请阅读《开发指南.md》。

---

## 1. 运行环境

- Windows 10/11；
- 已安装 **Node.js ≥ 22**（`EmailViewer.exe` 首次运行会自动安装依赖并构建，约 1~5 分钟）；
- 数据源二选一（详见第 4 节）：**推荐「IMAP 云端直连」**，任何邮箱都适用，只需一次性准备「应用密码 / 授权码」；
- 可选：本机安装**经典 Outlook（classic）桌面客户端并已登录目标邮箱**——这是替代方案「经典 Outlook 桌面」的前提（该方式**无需邮箱密码**）。注意：**新版 Outlook（`1.2026.x`，WebView2 架构）没有 COM 自动化接口，不能作为数据源**，用新版 Outlook 时请选 IMAP 云端直连。

## 2. 启动与停止（单文件 EXE）

所有启停动作都由 **`EmailViewer.exe`** 一个文件完成，**不再需要任何批处理或脚本启动器**：

| 操作 | 方式 |
|---|---|
| **启动并打开界面** | 双击 `EmailViewer.exe`（无参数）：首次会自动安装依赖（可见进度窗口），随后**后台无窗口启动服务**并打开浏览器 `http://127.0.0.1:3869` |
| 仅后台启动（供开机自启） | `EmailViewer.exe silent`（静默启动服务，不打开浏览器） |
| 带控制台启动（排错） | `EmailViewer.exe debug`（保留可见控制台窗口，实时看日志） |
| **停止服务** | `EmailViewer.exe stop`（结束占用 **3869** 端口的服务进程） |
| 查看状态 | `EmailViewer.exe status`（弹窗：是否运行、地址、Node 路径、目录） |

- 界面地址固定为 **`http://127.0.0.1:3869`**（**3869 就是服务端口**，仅监听本机回环地址，不对局域网/公网开放）；数据在 `server\data`，服务日志 `server\data\server.log`，启动器（启停 / 自启动作）日志 `launcher.log`。
- **开机自启**：默认开启，注册表 `HKCU\...\Run → StudentMailViewer` 指向 `"…\EmailViewer.exe" silent`；可在「设置 → 启动与通知」开关并查看实际状态。
- 依赖：本机需 Node.js ≥ 22；EXE 仅是启动器（约 12KB，用系统自带 .NET 4.x），服务本体仍是 Node。

## 3. 界面导览

顶部七个视图：

| 视图 | 作用 |
|---|---|
| **首页** | 一行多列（**推荐活动 / 重要邮件 / 临近截止 / 未读速览 / 低相关邮件**）：AI 判断“是否值得读/参加”，**每封邮件只出现在一列**；卡片悬停点「已阅」即归档并隐藏；左下角「**重要已阅**」可找回/取消；左侧仍可选账户 |
| **邮箱** | 左侧账户与文件夹树，中间邮件列表，右侧阅读窗格 |
| **智能收件箱** | 跨账户统一查看收件箱，按分类/账户/**标签**筛选 |
| **AI 助手** | 聊天式操作邮箱：可直接读取压缩后的邮件内容；读取范围可选「仅附带邮件 / 自动检索相关 / 最近未读」（只读，不会发送邮件） |
| **附件库** | 附件汇总：按类型/杂项分组，可排序（时间/大小）、预览、保存 |
| **日历** | 内置日历；「**从邮件智能安排**」一键把 AI/规则识别到的截止、考试、活动时间批量建事件（自动去重、限量） |
| **设置** | 账户、同步、AI、规则、汇总提醒、**启动与通知**、存储、外观、日志 |

顶部右侧：刷新（同步全部账户）、添加账户、提醒铃铛、深色/浅色切换。

## 4. 添加邮箱账户

右上角 **「添加账户」** 进入向导。

> ⚠️ **新版 Outlook（`1.2026.x`，WebView2 架构）没有 COM 自动化接口，不能作为数据源**；但你的 `xxx@outlook.com` 邮箱本质在云端，用 **IMAP 云端直连**即可同步同一个邮箱，并可与新版 Outlook 并行使用（两端看到的是云端同一封邮件，已读状态互通），**不需要新版 Outlook 做任何额外操作**。

### 4.1 方式一：IMAP 云端直连（**推荐 / 默认方式**，新版 Outlook / 个人 Outlook.com 都适用）

需要一次性的「两步验证 + 应用密码」，**只需做一次，大约 3 分钟**：

**第一步：开启两步验证（若尚未开启）**
1. 浏览器打开 <https://account.microsoft.com/security>，用 `you@outlook.com` 登录；
2. 进入 **“安全”页 → 高级安全选项**（Advanced security options）；
3. 若页面提示“两步验证未开启”，点 **开启**，按提示用手机号/备用邮箱接收验证码完成绑定；
4. 以后登录微软服务时除了密码还会要验证码，这是应用密码的前提。

**第二步：生成应用密码**
1. 仍在 **高级安全选项** 页面；
2. 找到 **“应用密码”**（App passwords）区块，点 **创建新应用密码**；
3. 会得到一串 **16 位字符**（形如 `abcd efgh ijkl mnop`），复制保存好；
4. 再次访问时页面通常不再完整显示它，因此请立即使用。

**第三步：在查看器添加账户**
1. 查看器右上角 **「添加账户」**（默认已在 IMAP 方式）；
2. 邮箱服务商选择 **“Outlook.com（个人微软账户）”**，邮箱地址填 `you@outlook.com`；
3. 密码处粘贴那串 **16 位应用密码**（含空格无妨，可自动忽略）；
4. 点 **「测试连接」** → 成功后 **「保存并开始同步」** 即可。

| 其它邮箱 | IMAP 服务器 | 端口 | 获取密码方法 |
|---|---|---|---|
| QQ 邮箱 | `imap.qq.com` | 993 | 网页版 设置→账户→开启 IMAP/SMTP→获取授权码 |
| 网易 163 | `imap.163.com` | 993 | 同上，开启后获取客户端授权码 |
| Gmail | `imap.gmail.com` | 993 | 开启两步验证后生成应用专用密码 |

### 4.2 方式二：经典 Outlook 桌面

如果本机安装有**经典 Outlook（classic，旧版）并且已在该客户端登录目标邮箱**时可用。**它不是新版 Outlook 的同步方式**：

1. 在向导选「经典 Outlook 桌面」，点 **「检测本机经典 Outlook 账号」**（首次自动拉起 Outlook 需 30~60 秒）；
2. 点选账号 → 保存并同步；标记已读/未读、星标会写回经典 Outlook；
3. 该方式**无需输入邮箱密码**（凭据由本机 Outlook 自己管理），代价是只能看到本机 Outlook 已缓存范围内的邮件；
4. 若检测不到、或你平时用的是**新版 Outlook**（`1.2026.x`，WebView2 架构，**没有 COM 自动化接口**），请改用 4.1 的 IMAP 方式。

### 4.3 账户管理（设置 → 邮箱账户）

- **主账户 / 副账户**：主账户在左栏始终展开文件夹；副账户折叠为“色点+未读数”，点击才展开（占界面少）。第一个添加的自动成为主账户，也可在设置里点「设为主」。
- 账户行提供：同步、编辑（名称/颜色/密码等）、打开邮箱、删除（连同本地同步数据）。
- 经典 Outlook 桌面账户在编辑页只允许改显示名称/颜色/启用状态（凭据由 Outlook 管理）；IMAP 账户可在编辑页更换密码。

## 5. 同步机制

- 邮件先同步「信封」：发件人、主题、时间、大小、是否有附件 —— 这一步快，列表马上可见；
- **正文与附件**由后台逐封抓取（也可在打开邮件时即时抓取那一封）；
- 「设置 → 同步」可调自动同步间隔（默认 10 分钟）、首轮回看天数（默认 30 天，IMAP 超过该时间的旧邮件不下载；经典 Outlook 桌面模式以本机 Outlook 缓存范围为准）；
- 右上角「刷新」手动立即同步全部账户；
- 顶栏与文件夹树的未读数每 10 秒刷新。

## 6. 阅读邮件

- 点击列表任意一封即可在右侧阅读；列表内可用 **↑/↓ 方向键**快速切换；
- 阅读窗格顶部的**渐变高亮按钮「让 AI 读这封邮件」**：把这封邮件一键交给 AI 助手（总结要点 / 提取待办与截止时间），针对性追问最快；
- 旁边的「**标记已阅**」：把处理完的邮件归档，**首页各列会立即隐藏它**；可在首页左下角「重要已阅」找回或取消已阅；
- 其余按钮：已读/未读（会写回 Outlook / IMAP 服务器）、星标、AI 重新分类、生成一句话摘要、加标签；
- **HTML 邮件**默认拦截远程图片（防止被追踪），需要时可点「加载远程图片」；
- 点「查看完整邮件头」可看技术细节（Message-ID、Reply-To 等）；
- 邮件底部自动展示：
  - **AI/本地一句话摘要**（若无 AI Key 也能用关键词摘要）；
  - **附件列表**：图片/PDF/文本可在线预览，其余可下载；「保存到本地文件夹」直接复制到设定目录（设置→存储）；
  - **AI 识别到的日程时间**：标注来源（AI 提取 / 规则识别），可单个「加入日历」，也可「**全部加入日历**」一次安排；
  - 该邮件已加入日历的事件，可直接删除。

## 7. 搜索、过滤、排序

列表顶部：
- 快速筛选：全部 / 未读 / 有附件 / 星标；
- 🔍 展开高级面板：主题与发件人关键词、正文包含、发件人包含、日期范围、**附件类型**、分类多选；
- 排序：时间 / 发件人 / 主题 / 未读 / 大小（升/降序）；
- 分组：按日期 / 按账户 / 按分类（“智能收件箱”默认按账户分组，方便跨账户对比）。

正文搜索只对“已抓取正文”的邮件生效（列表摘要为空即尚未抓取，稍候即自动抓取）。

## 8. 高级过滤规则（自动处理）

「设置 → 过滤规则」里可建立规则：满足 **条件组合** 后自动执行 **动作**。

常用示例（可直接照抄）：

| 规则名 | 条件 | 动作 |
|---|---|---|
| 课程助教通知 | 发件人包含「助教」**且**发件人包含「课程」 | 添加标签 `课程`；归类 `课程通知` |
| 成绩发布 | 主题包含「成绩」**且**发件人包含「系统」 | 归类 `成绩发布`；加星标 |
| 垃圾拦截 | 正文包含「中奖」**或**发件人包含「@promo.com」 | 归类 `垃圾邮件` |
| 作业截止提醒 | 主题包含「作业」或「deadline」 | 归类 `作业提交`；加星标 |

支持条件：发件人/收件人/抄送/主题/正文/任意位置包含、有附件、附件类型、所在文件夹、账户、未读。
支持动作：打标签、归类、标记已读/未读、加星/去星。
新建后可点「**应用到已下载邮件**」立即重算存量邮件（正文需已抓取）。

## 9. AI 智能（分类与摘要）

「设置 → AI 智能」开启后，新抓取正文的邮件会被后台自动分类+摘要。支持任意 **OpenAI 兼容** 接口：

| 服务商 | Base URL | 示例模型 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` |
| Moonshot Kimi | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| 本机 Ollama | `http://127.0.0.1:11434/v1` | `qwen2.5:7b`（Key 随意） |

填好后点「测试」，通过即保存。**没有 Key / 关闭 AI 时不影响使用**——内置中文关键词规则会做基础分类与摘要。个别邮件可点阅读区的 ✨ 手动重新分类。

**密钥的两种存放方式（推荐 .env）**：

1. **写在 `server\.env`（推荐）** —— 打开 `emailviewer\server\.env`，按 `AI_API_KEY_服务商名大写` 的格式填写，例如：

   ```ini
   AI_API_KEY_GATEWAY=sk-你的密钥
   AI_API_KEY_DEEPSEEK=sk-你的密钥
   ```

   服务商名就是设置页里显示的小写英文名（内置的 `deepseek` / `openai` / `kimi` / `glm` / `qwen` / `ollama`，自定义的 `gateway` 等）。也可以只写一个通用的 `AI_API_KEY` 作为所有服务商的兜底。**改完需要重启服务**（`EmailViewer.exe stop` 后再启动）。

   这种方式的好处：密钥不落在数据库里，`.env` 已被 `.gitignore` 忽略不会误提交，适合长期使用。配置生效后，设置页对应服务商右侧会显示「**由 .env 管理**」，其密钥输入框同时变为禁用——要换 Key 请直接改 `.env`，在界面上输入不会被保存（会弹出提示）。

2. **在设置页直接填（备用）** —— 适合临时试用。这种方式保存的 Key 会**以明文存在本机数据库**（`server\data\mailview.db`）里，仅靠本机文件权限保护；多人共用的电脑上建议改用 `.env`。

优先级：**进程环境变量 > `server\.env` > 设置页保存的值**。也就是说 `.env` 里写了某个服务商的 Key，设置页里同名服务商的输入就会被覆盖（界面会标注来源）。

**密钥与模型的操作方式**：
- 密钥框**默认是空的**；已保存的密钥以「前 4 位 + * + 后 4 位」形式显示（例如 `sk-w************Afjk`）。输入框留空即保持原密钥不变，粘贴新值则替换；
- **获取模型列表**：点模型行右侧「获取模型列表」，会自动调用该服务商的 `/models` 接口拉取可用模型，随后可在下拉里选择；不想用列表时也可以直接在输入框里手写模型名（自定义）。

**模块增删**：
- **添加自定义服务商**：点「添加自定义 OpenAI 兼容服务商」，填 名称 / Base URL / 模型名 / Key（任意中转或自建网关都行），添加后会自动设为当前服务商；
- **删除服务商**：每个服务商卡片右上角有「删除」，删除后其 Key 一并移除，当前服务商被删会自动切到剩余的第一个；
- **恢复内置列表**：点「恢复内置服务商」，重新生成 DeepSeek / OpenAI / Kimi / GLM / 通义 / Ollama 预设（已填写的 Key 会保留；自定义服务商需重新添加）。

分类枚举：课程通知 / 作业提交 / 成绩发布 / 社团活动 / 系统提醒 / 营销推广 / 垃圾邮件 / 个人往来 / 其他。

## 10. 日历与提醒

- 在阅读邮件时点「加入日历」即可创建事件；可设是否全天、时长与提醒时机（提前 2 小时 / 1 天 / 1 天+2 小时）；
- 「日历」页可新建/删除事件，或把事件导出为 `.ics`（可导入 Google 日历 / Outlook 日历等，一次性手动同步）；
- 「设置 → 汇总提醒」：每天固定时间（默认 21:00）把各账户收件箱 **30 小时内的未读重点邮件**（作业/成绩/课程/社团分类，可在设置里勾选）汇成一份提醒，铃铛里查看。

## 11. 附件与存储

- 「附件库」：按类型汇总、文件名搜索、只看大附件（>10MB）开关；预览支持图片 / PDF / 文本 / CSV / JSON 等，Office 文档提供下载；
- **排序**：默认「时间（最新在前）」，可切换「时间（最早在前）/ 大小（大到小）/ 大小（小到大）」，选择会被记住；
- **附件只缓存一次**：同一份内容（按内容哈希判定）在本机只落盘一份——即使邮件被重复抓取或多次同步，也不会重复占用磁盘；启动与每小时维护时会自动删除无引用的历史遗留缓存文件；
- 「保存到本地文件夹」需在「设置 → 存储」里配置一个可写目录（如 `C:\Users\你的名字\Documents\邮件附件`）并“测试并保存”，之后可一键保存、可选自动打开所在文件夹；
- 附件缓存有大小上限（默认 400MB），超出自动清理最旧缓存（邮件正文不受影响，再次打开会重新抓取附件）；也可以手动清空缓存。

## 12. 外观

右上角太阳/月亮按钮或「设置 → 外观」切换：深色（护眼）/ 浅色 / 跟随系统。

同一页面还有 **「在邮箱界面显示草稿箱」** 开关：本查看器不提供写信功能，所以**草稿箱默认隐藏**（同步时也会跳过，省时省资源）；需要时打开开关即可显示并恢复同步。

## 13. AI 助手（Chatbox）

顶栏「AI 助手」是一个聊天界面，用来“用自然语言操作邮箱”：

- **附带邮件再提问**：在邮箱里选中邮件后切到 AI 助手，会自动把它作为上下文（顶部可见 chip，可移除/清空）。支持一次附带动最近几封；
- **快捷指令**：总结附带邮件 / 提取截止时间 / 按重要性排序 / 未读重点有哪些；
- **两种模式**（界面右上角会显示当前模式）：
  - **AI 模式**：调用「设置 → AI 智能」里配置的服务商（如默认网关 `qwen3.8-max-0902`）回答，能读你附带的邮件正文；
  - **本地检索模式**：当 AI 未启用或 Key 无效时，自动把你的问题当关键词在本地邮件里检索，并以可点击的结果卡片返回——点卡片直接跳到那封邮件；
- 对话历史保存在本机浏览器（localStorage），可一键清空；
- **只读**：助手只做阅读、总结、检索与建议，不会发送、回复或删除任何邮件。

## 14. 新邮件通知与提醒中心

- 同步发现新邮件后：顶栏铃铛出现红点，条目形如「`账号` 收到 N 封新邮件」，并尽量弹出**系统通知**（首次使用需允许浏览器通知权限）；
- **点击通知**会直接打开最新那一封邮件；
- 提醒中心同时汇总：日历事件提醒、每日未读重点汇总；
- 开关：「设置 → 启动与通知」里的 **新邮件通知**（默认开启）。若不想被打扰可关闭，仅保留铃铛条目。

## 15. 数据、隐私与迁移

- 数据全部在本机：`server\data\mailview.db`（邮件库）、`attachments/`（附件缓存）、`server.log`（日志）；
- 迁移到新电脑：拷贝整个 `emailviewer` 目录即可（含 `EmailViewer.exe`）；但「经典 Outlook 桌面」模式仍要求新电脑装有**经典 Outlook 并登录同账号**，改用推荐的 IMAP 云端直连则没有这个限制；
- 彻底删除一切数据：删除 `server\data` 目录后重启。

## 16. 常见问题（FAQ）

**Q：双击 `EmailViewer.exe` 后窗口一闪而过（或干脆没有窗口）？**
这是正常的——无参数启动会在依赖就绪后**无窗口后台启动服务**并打开浏览器。想确认是否成功：打开 `http://127.0.0.1:3869`，或看 `server\data\server.log`（启动器自身的日志在 `launcher.log`）；想看到实时日志排错，请改用 `EmailViewer.exe debug`（保留可见控制台窗口）。停止服务用 `EmailViewer.exe stop`，查看运行状态用 `EmailViewer.exe status`。

**Q：开机自启怎么关？**
「设置 → 启动与通知」关掉开关即可（会同时删除注册表项）；页面上会显示注册表的实际状态，便于确认。

**Q：端口被占用？**
本服务默认使用 **3869** 作为服务端口（也就是界面地址 `http://127.0.0.1:3869` 的端口）。若被占用可设置环境变量 `MAILVIEW_PORT=xxxx` 后运行；注意不要用 `PORT`（会被系统占用变量干扰）。也可以先用 `EmailViewer.exe stop` 清掉还在跑的旧实例。

**Q：我用的“新版 Outlook”能直接连查看器吗？**
不能。**新版 Outlook（`1.2026.x`，WebView2 架构）没有 COM/MAPI 自动化接口**，因此查看器无法读取它的本地缓存。请用「添加账户 → **IMAP 云端直连**」（**推荐、默认方式**）：为邮箱开启两步验证并生成应用密码后即可同步，且与你平时使用新版 Outlook 互不影响（两端看到的都是云端同一封邮件，状态互通）。只有当本机装的是**经典 Outlook（classic）并已登录**时，才可用「经典 Outlook 桌面」这一替代方式。

**Q：检测经典 Outlook 很慢 / 失败？**
仅当你想用「经典 Outlook 桌面」这个**替代方式**时才需要此项（**前提是本机装有经典 Outlook 且已登录该账号**）；首次自动启动可能需要 30~60 秒，请耐心等待并确保没有 Outlook 弹窗（如“是否允许此程序访问邮箱”之类，点允许并勾选“不再提示”）。若反复失败，或你平时用的是新版 Outlook（**没有 COM 接口，无法自动化**），直接改用推荐方式 IMAP 云端直连即可。

**Q：看不到某些邮件？**
经典 Outlook 桌面模式：本机 Outlook 缓存范围之外（过老的邮件或未下载的云端邮件）不可见；IMAP 模式：首轮只同步设置的回看天数（默认 30 天），可在「设置 → 同步」调大后重新同步。

**Q：正文为空/要等很久？**
打开邮件时会即时向 Outlook/服务器抓取该封正文；后台也在限速预取，避免一次抓太多。稍候或手动点开即好。

**Q：AI 分类不准？**
在邮件菜单点“用 AI 重新分类”；或到设置确认已启用 AI 且默认服务商 Key 正确；无 AI 时关键词规则比较保守，可在「过滤规则」用自定义规则补充。

**Q：如何完全卸载？**
删除整个 `emailviewer` 目录即可（本程序不写注册表、不装系统服务）。
